# Core runtime dependencies — required to run drone.py / main.py / scripts
pymavlink==2.4.49
opencv-python==5.0.0.93
mediapipe==0.10.14

# SITL / simulation tooling (see README "Common first-run dependency gaps")
# Not imported by this project's code directly, but needed to run MAVProxy/SITL locally
empy==3.3.4
MAVProxy==1.8.74
future==1.0.0
matplotlib==3.11.1

# Raspberry Pi camera support — only installs on the Pi (aarch64), skipped on the
# development computer. Also requires the libcamera Python bindings built from
# source on the Pi itself. I had to get Claude to help me with this, it was way
# above my skillset!!
picamera2==0.3.37; platform_machine == "aarch64"